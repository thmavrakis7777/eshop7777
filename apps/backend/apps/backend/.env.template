STORE_CORS=http://localhost:3000,https://docs.medusajs.com
ADMIN_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
AUTH_CORS=http://localhost:5173,http://localhost:9000,http://localhost:3000,https://docs.medusajs.com
REDIS_URL=redis://localhost:6379
JWT_SECRET=supersecret
COOKIE_SECRET=supersecret
DATABASE_URL=
DB_NAME=medusa-backend

# SendGrid dashboard -> Settings -> API Keys. Order-confirmation emails
# (src/subscribers/order-placed.ts) and password-reset emails
# (src/subscribers/auth-password-reset.ts) degrade to a logged no-op when
# unset — see CHECKOUT_PREMIUM_SPEC.md Phase 5.
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=

# The storefront's own public URL — used only to build the link inside the
# password-reset email (src/subscribers/auth-password-reset.ts). Not a
# secret. Matches the storefront origin already in STORE_CORS above.
STOREFRONT_URL=http://localhost:3000
